#include "ServicePool.h"

namespace AppSystem{

    /*template<typename S> 
        std::shared_ptr<S> ServicePool::getSingleService() {
            return ioc->resolve<S>();
        }*/
    /*template<typename S> 
        std::vector<std::shared_ptr<S>> ServicePool::getServices() {
            return ioc->resolve_by_type<S>();
        }*/
}